# Nested Claude invocations always require contextual approval review when
# they need to run outside the Codex sandbox.

prefix_rule(
    pattern = ["claude"],
    decision = "prompt",
    justification = "Nested agents require review because their own sandbox and approval settings are the remaining containment boundary.",
    match = ["claude -p summarize this repository", "claude --print review this diff"],
)
