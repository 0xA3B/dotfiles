# Fish loads startup modules from conf.d before sourcing this file.
#
# Keep this file minimal and use conf.d for ordered startup concerns such as:
#   - base environment bootstrap
#   - shell integrations (direnv, zoxide, starship, etc.)
#   - interactive shortcuts and bindings
